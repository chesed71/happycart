# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Env files (anon key는 비밀이 아니지만 환경별로 별도 관리)
# service_role 키는 이 디렉터리의 어떤 파일에도 두지 않는다 — 여기 파일은 Flutter
# asset 으로 APK 에 번들되므로 공개값(anon key)만 담을 것.
.env
.env.development
.env.staging
.env.production
# .env.example 은 커밋 가능 (예외)
!.env.example

# fastlane — Play Developer API service account 키는 절대 커밋 금지(비밀).
# 키는 repo 밖 ~/.config/happycart/play-service-account.json 에 보관하지만,
# 실수로 repo 안에 두더라도 커밋되지 않도록 패턴으로 이중 방어한다.
**/play-service-account*.json
# fastlane 실행 생성물.
android/fastlane/report.xml
android/fastlane/Preview.html
android/fastlane/README.md
android/fastlane/screenshots
android/fastlane/test_output

# Firebase Hosting 배포 캐시.
.firebase/

