name: happycart_rules
description: "HappyCart MVP — 식품 영양 평가 규칙 (verdict & headline) 단일 소스 패키지."
version: 1.0.0

environment:
  sdk: ^3.11.5

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
